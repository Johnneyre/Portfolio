---
import { useTranslations } from "../i18n/utils";
import type { SupportedLocale } from "../i18n/utils";

interface Props {
  locale: SupportedLocale;
}

const { locale } = Astro.props;
const t = useTranslations(locale);
---

<footer
  class="border-t"
  style="border-color: var(--color-surface-700); background: var(--color-surface-950);"
>
  <div class="container-max flex flex-col sm:flex-row items-center justify-between gap-4 py-7 px-5">
    <!-- Left: copyright -->
    <p class="text-sm m-0" style="color: var(--color-text-muted);">
      &copy; <span id="footer-year"></span> Johnneyre Zambrano
    </p>

    <!-- Right: nav links -->
    <nav>
      <ul class="flex items-center gap-6 list-none m-0 p-0">
        <li>
          <a href="#inicio" class="link-hover text-sm font-medium" data-i18n="footer.links.home">
            {t.footer.links.home}
          </a>
        </li>
        <li>
          <a href="#experience" class="link-hover text-sm font-medium" data-i18n="footer.links.experience">
            {t.footer.links.experience}
          </a>
        </li>
        <li>
          <a href="#projects" class="link-hover text-sm font-medium" data-i18n="footer.links.projects">
            {t.footer.links.projects}
          </a>
        </li>
      </ul>
    </nav>
  </div>
</footer>

<script>
  const el = document.getElementById("footer-year");
  if (el) el.textContent = new Date().getFullYear().toString();
</script>
